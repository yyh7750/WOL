<Window x:Class="WOL.View.RemoteExplorerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remote Explorer"
        Width="1700"
        Height="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="8" Orientation="Horizontal">
            <TextBlock Margin="0,0,6,0"
                       VerticalAlignment="Center"
                       Text="Client:" />
            <TextBox Width="220"
                     Margin="0,0,8,0"
                     Text="{Binding ClientHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0,0,6,0"
                       VerticalAlignment="Center"
                       Text="Port:" />
            <TextBox Width="80"
                     Margin="0,0,8,0"
                     Text="{Binding ClientPort}" />
            <Button Margin="0,0,8,0"
                    Command="{Binding ConnectCommand}"
                    Content="Connect" />
            <Button Width="30"
                    Margin="0,0,8,0"
                    Command="{Binding GoUpCommand}"
                    Content="↑" />
            <TextBox Width="520"
                     Margin="0,0,8,0"
                     Text="{Binding CurrentPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Width="80"
                    Command="{Binding NavigateCommand}"
                    Content="Go" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" ItemsSource="{Binding TreeRoots}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <ListView Grid.Column="1"
                      ItemsSource="{Binding Entries}"
                      SelectionMode="Extended">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_DoubleClick" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsParentFolder}" Value="True">
                                <Setter Property="Background" Value="LightBlue" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="320"
                                        DisplayMemberBinding="{Binding Name}"
                                        Header="Name" />
                        <GridViewColumn Width="110"
                                        DisplayMemberBinding="{Binding Type}"
                                        Header="Type" />
                        <GridViewColumn Width="110"
                                        DisplayMemberBinding="{Binding SizeDisplay}"
                                        Header="Size" />
                        <GridViewColumn Width="200"
                                        DisplayMemberBinding="{Binding ModifiedLocal}"
                                        Header="Modified (Local)" />
                        <GridViewColumn Width="400"
                                        DisplayMemberBinding="{Binding FullPath}"
                                        Header="Full Path" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <DockPanel Grid.Row="2" Margin="8">
            <TextBlock VerticalAlignment="Center"
                       DockPanel.Dock="Left"
                       Text="{Binding SelectionInfo}" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Width="100"
                        Margin="8,0,0,0"
                        Command="{Binding OkCommand}"
                        Content="Select Files" />
                <Button Width="100"
                        Margin="8,0,0,0"
                        Command="{Binding CancelCommand}"
                        Content="Cancel" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>


